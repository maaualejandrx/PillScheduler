buildscript {
    ext.kotlin_version = '1.8.21' // Define la versión de Kotlin.
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.0.2" // Versión de Android Gradle Plugin.
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version" // Complemento Kotlin.
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Configuración de directorios de compilación.
rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

// Tarea para limpiar el proyecto.
tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
